"""
==config.py==
Contains the class 'Config' which, well, sets up all the config parameters
for Flask and Flask-related modules.
"""
class Config:
    """
    Contains all config parameters.
    """
    SECRET_KEY = "secret_key"
